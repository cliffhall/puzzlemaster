export enum WindowAPIMethods {
  IS_MAXIMIZED = "is-maximized",
}

export enum WindowEvents {
  WINDOW_STATE = "window-state",
}

export enum WindowStates {
  MAXIMIZED = "window-maximized",
  UNMAXIMIZED = "window-unmaximized",
}
